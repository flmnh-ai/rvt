% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blend.R
\name{rvt_vat}
\alias{rvt_vat}
\title{Visualization for Archaeological Topography (VAT)}
\usage{
rvt_vat(
  dem,
  res_x,
  res_y,
  terrain = "general",
  svf_n_dir = 16L,
  svf_r_max = NULL,
  svf_noise = NULL,
  hs_sun_el = NULL,
  ve_factor = NULL,
  verbose = TRUE
)
}
\arguments{
\item{dem}{Numeric matrix of elevations.}

\item{res_x}{Pixel size in X (metres).}

\item{res_y}{Pixel size in Y (metres).}

\item{terrain}{Terrain preset: \code{"general"}, \code{"flat"}, or \code{"steep"}.}

\item{svf_n_dir}{SVF search directions (default 16).}

\item{svf_r_max}{SVF search radius in pixels (\code{NULL} = terrain preset).}

\item{svf_noise}{SVF noise level 0--3 (\code{NULL} = terrain preset).}

\item{hs_sun_el}{Hillshade sun elevation in degrees (\code{NULL} = terrain preset).}

\item{ve_factor}{Vertical exaggeration. If \code{NULL} (default), uses the
terrain preset's \code{ve_default} (e.g. 3 for \code{"flat"}). This differs from
rvt-py, which always defaults to 1 regardless of preset.}

\item{verbose}{Print progress messages? (default TRUE)}
}
\value{
Numeric matrix of blended values [0, 1].
}
\description{
Blends hillshade, slope, positive openness, and sky-view factor into a
single composite following Kokalj & Somrak (2019). Layer order and blend
modes match the rvt-py "Archaeological" preset.
}
\examples{
# Synthetic 100x100 DEM with a mound
dem <- outer(1:100, 1:100, function(x, y)
  5 * exp(-((x - 50)^2 + (y - 50)^2) / 200))
result <- rvt_vat(dem, 1, 1, terrain = "flat", verbose = FALSE)
}
